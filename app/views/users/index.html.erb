
      <h2>User info</h2>


  <tr>
	  <th>name</th>
  </tr>
  <tr>
	  <th>introduction</th>
	</tr>
<div class="row">
  <a class="btn btn-outline-secondary btn-block fas fa-user-cog edit_user_65983" href="/users/65983/edit"></a>
</div>

  <h2>New Book</h2>
         <%= form_with model: Book.new do |f| %>
        <h4>Title</h4>
         <%= f.text_field :title %>
        <h4>Opinion</h4>
         <%= f.text_area :body %>
         <%= f.submit 'Create Book' %>
         <% end %>

</form>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>
      <table class="table">
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <% @users.each do |user| %>
   <td><%= user.profile_image %></td>

      <td><%= user.name %></td>

      <%= link_to "Show",user_path(user.id) %>
 <% end %>

